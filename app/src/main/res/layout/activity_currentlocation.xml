<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/currentLocation_coordinatorLayout_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CurrentLocationActivity">

    <LinearLayout
        android:id="@+id/currentLocation_linearLayout_default"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:padding="100dp"
            android:text="@string/currentLocation_permissionError" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/currentLocation_linearLayout_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="10dp"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/currentLocation_textView_lastKnownLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/currentLocation_lastKnownLocation"
            tools:text="@string/currentLocation_lastKnownLocation"
            android:layout_marginBottom="10dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/currentLocation_textView_locations"
            tools:text="Locations list: "
            />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>